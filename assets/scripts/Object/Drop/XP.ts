import { _decorator, CCFloat, CCInteger, Component, Node, tween, UIOpacity, Vec2, Vec3 } from 'cc';
import { Drop } from './Drop';
const { ccclass, property } = _decorator;

@ccclass('XP')
export class XP extends Drop {
}